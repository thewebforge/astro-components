---
interface Props {
  class: string
  src?: string
  alt?: string
}

const { class: classNames, src = 'https://picsum.photos/seed/astro/640/480', alt = '' } = Astro.props
---

<img class={classNames} src={src} alt={alt} loading="lazy" decoding="async" />